erDiagram
    building {
        int id PK
        varchar name
    }

    classroom {
        int id PK
        varchar name
        int building_id FK
    }

    schedule {
        int id PK
        varchar day
        varchar start_time
        varchar end_time
    }

    faculty {
        int id PK
        varchar name
    }

    course {
        int id PK
        varchar name
        int faculty_id FK
    }

    program {
        int id PK
        varchar name
        int faculty_id FK
    }

    role {
        int id PK
        varchar name
    }

    user {
        int id PK
        varchar name
        varchar last_name
        int role_id FK
    }

    course_schedule {
        int id PK
        int course_id FK
        int schedule_id FK
        int classroom_id FK
        int professor_id FK
        int capacity
    }

    program_course {
        int course_id FK
        int program_id FK
        int curriculum_semester
    }

    student_program {
        int program_id FK
        int user_id FK
        int current_semester
    }

    student_course {
        int course_schedule_id FK
        int user_id FK
    }

    building ||--o{ classroom : contains
    classroom ||--o{ course_schedule : assigns
    schedule ||--o{ course_schedule : defines
    faculty ||--o{ course : offers
    faculty ||--o{ program : has
    course ||--o{ course_schedule : programs
    course ||--o{ program_course : belongs_to
    program ||--o{ program_course : includes
    role ||--o{ user : classifies
    user ||--o{ student_program : enrolled_in
    program ||--o{ student_program : has
    user ||--o{ student_course : takes
    course_schedule ||--o{ student_course : registers